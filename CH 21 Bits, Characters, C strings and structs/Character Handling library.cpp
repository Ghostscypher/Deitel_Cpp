/**
 * @brief We use C++ library cctype
 * i.e. 
 * 		#include <cctype>
 * 
 * @description
	 *Prototype Description
 *		int isdigit( int c ) 	Returns 1 if c is a digit and 0 otherwise.
 *		int isalpha( int c ) 	Returns 1 if c is a letter and 0 otherwise.
 *		int isalnum( int c ) 	Returns 1 if c is a digit or a letter and 0 otherwise.
 *		int isxdigit( int c ) 	Returns 1 if c is a hexadecimal digit character and 0 otherwise.
								(See Appendix D, Number Systems, for a detailed explanation of
								binary, octal, decimal and hexadecimal numbers.)
 *		int islower( int c ) 	Returns 1 if c is a lowercase letter and 0 otherwise.
 *		int isupper( int c ) 	Returns 1 if c is an uppercase letter; 0 otherwise.
 *		int tolower( int c ) 	If c is an uppercase letter, tolower returns c as a lowercase letter.
								Otherwise, tolower returns the argument unchanged.
 *		int toupper( int c ) 	If c is a lowercase letter, toupper returns c as an uppercase letter.
								Otherwise, toupper returns the argument unchanged.
 *		int isspace( int c ) 	Returns 1 if c is a white-space character—newline ('\n'), space
								(' '), form feed ('\f'), carriage return ('\r'), horizontal tab
								('\t'), or vertical tab ('\v')—and 0 otherwise.
 *		int iscntrl( int c ) 	Returns 1 if c is a control character, such as newline ('\n'), form
								feed ('\f'), carriage return ('\r'), horizontal tab ('\t'), vertical
								tab ('\v'), alert ('\a'), or backspace ('\b')—and 0 otherwise.
 *		int ispunct( int c ) 	Returns 1 if c is a printing character other than a space, a digit, or
								a letter and 0 otherwise. 
*/

int main()
{
	
} // End main